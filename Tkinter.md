Tkinter

Basic commands

    - Ouverture fenetre
        window = Tk()
        window.title("Titre de la fenêtre")
        window.mainloop()
    
    - Button
        button = Button(window, text="Click", command=fonction, justify= LEFT)
        button.pack()
    
    - Label -> Print un text
